# Core Flask Dependencies (Fast install)
Flask==2.3.3
Flask-CORS==4.0.0
PyJWT==2.8.0
requests==2.31.0
Werkzeug==2.3.7

# API & Communication (Fast install)
assemblyai>=0.30.0
flask-socketio==5.3.6
websocket-client==1.6.4

# Audio Processing (Minimal for Vercel size limits)
pydub==0.25.1

# Lightweight Scientific Computing
numpy>=1.24.3

# Development Dependencies (optional for local development)
# python-dotenv==1.0.0  # Not needed on Vercel (platform handles env vars)
# gunicorn==21.2.0      # Not needed on Vercel (platform handles WSGI)

# Heavy Dependencies - Commented out for Vercel 250MB limit
# scipy>=1.10.0  # Large package that may cause size issues

# Optional: Advanced ML dependencies (very large, use only locally)
# torch>=1.13.0
# nemo-toolkit[all]==1.22.0
